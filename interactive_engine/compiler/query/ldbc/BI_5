MATCH (tag:TAG {name: 'Gough_Whitlam'})<-[:HASTAG]-(message:POST)-[:HASCREATOR]->(person:PERSON), 
      (message)<-[likes:LIKES]-(:PERSON), 
      (message)<-[:REPLYOF]-(reply:COMMENT)
Return count(tag);
