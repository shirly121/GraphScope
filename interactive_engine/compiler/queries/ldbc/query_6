MATCH (person:PERSON {id:4026})-[:KNOWS*1..3]-(other)<-[:HASCREATOR]-(post:POST)-[:HASTAG]->(tag:TAG {name: "North_German_Confederation"}),
      (post)-[:HASTAG]->(otherTag:TAG)
WHERE otherTag <> tag
Return count(person);
