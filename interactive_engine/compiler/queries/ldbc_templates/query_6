MATCH (person:PERSON {id:$_id_})-[:KNOWS*1..3]-(other)<-[:HASCREATOR]-(post:POST)-[:HASTAG]->(tag:TAG {name: $_tag_}),
      (post)-[:HASTAG]->(otherTag:TAG)
WHERE otherTag <> tag
Return count(person);
